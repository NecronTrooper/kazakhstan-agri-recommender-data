# Словарь данных: `output/master_dataset.csv`

Формируется `06_merge.py` из скриптов 01 (FAOSTAT), 02 (World Bank), 03 (ERA5), 04 (SoilGrids), 05 (stat.gov.kz). Актуально на 2026-09-10.

**Размер:** 382 строки × 44 столбца
**Гранулярность строки:** (область, культура, год) — 3 области × 6 культур (не все культуры есть во всех областях) × до 36 лет (1990–2025)

---

## Важно понять перед использованием

Не все столбцы варьируются на уровне строки. Три группы признаков имеют **более грубую** гранулярность, чем сама таблица, и просто "размножены" на каждую строку:

| Группа столбцов | Реально варьируется по | Не варьируется по |
|---|---|---|
| Мировые цены (`wheat_hrw_usd_t` … `urea_fertilizer_usd_t`) | году | региону, культуре |
| Климат (`gs_*`) | году | региону, культуре |
| Почва (`soil_*`) | — (одно значение на весь датасет) | году, региону, культуре |

**Почему:** климат (03) усредняется по одному bounding box'у на весь зерновой пояс сразу (не по области отдельно), почва (04) усредняется по всем 20 точкам сетки в одно число. Это осознанное упрощение — см. `README.md`, раздел про районный уровень и обоснование, почему точечная детализация климата/почвы была бы методологически слабее нынешнего среднего. **Для моделирования это значит:** межрегиональная вариация урожайности в этом датасете объясняется агротехникой/сортом/качеством отчётности региона, а НЕ климатом или почвой — они здесь работают только как межгодовой (не межрегиональный) сигнал.

---

## Идентификаторы и метаданные

| Столбец | Тип | Единица | Описание | Пропуски |
|---|---|---|---|---|
| `year` | int | год | Год наблюдения | 0% |
| `region` | str | — | Область: `Акмолинская область`, `Костанайская область`, `Северо-Казахстанская область` | 0% |
| `crop` | str | — | Культура (рус.): `Пшеница`, `Ячмень`, `Кукуруза`, `Подсолнечник`, `Зерновые и бобовые (группа)`, `Масличные (группа)`. Две последних — агрегаты по Акмолинской области (см. ниже), не сопоставимы напрямую с отдельными культурами | 0% |
| `crop_en` | str | — | Культура (англ., для джойна с ценами): `Wheat`, `Barley`, `Maize`, `Sunflower seed`, `Grains and legumes (group)`, `Oilseeds (group)` | 0% |
| `source` | str | — | `stat_gov_kz_real` — отдельная культура (реальные данные); `stat_gov_kz_real_group` — агрегированная группа культур (только Акмолинская, ячмень/кукуруза которой не разбиты по культурам у источника) | 0% |

## Целевые переменные (производство, stat.gov.kz)

| Столбец | Тип | Единица | Описание | Пропуски | Диапазон |
|---|---|---|---|---|---|
| `area_ha` | float | га | Посевная площадь | 11.3% | 1.5 – 4 915 073 |
| `production_ton` | float | т | Валовой сбор | 1.0% | 0 – 7 341 280 |
| `yield_kgha` | float | кг/га | Урожайность (=production_ton×1000/area_ha, публикуется источником напрямую, не пересчитывается) | 2.4% | 60 – 4080 |

**Откуда пропуски:** не случайны, а прослеживаются до конкретной причины — по каждой стоит свериться с `Отчёт_о_сборе_данных.md`, §2.5, если пропуск попал в выборку для анализа:
- `area_ha` пропущена целиком для Акмолинская/Подсолнечник (все 35 лет) — площадь подсолнечника не разбита отдельно от группы масличных у источника (реальное ограничение, не баг).
- Акмолинская/Пшеница — пропуски за 1991–1998 (детектор скачка масштаба отбросил ранний период файла как данные в неизвестной единице измерения).
- Прочие точечные пропуски — годы, где у конкретного (регион, культура) не было данных в исходной таблице (например, "-" в ячейке источника).

## Мировые цены (World Bank Pink Sheet, скрипт 02)

Все — USD/т, **варьируются только по `year`**, одинаковы для всех регионов/культур в один год (среднегодовые, посчитаны из помесячных).

| Столбец | Описание | Пропуски | Причина пропусков |
|---|---|---|---|
| `wheat_hrw_usd_t` | Пшеница, US Hard Red Winter | 0% | — |
| `wheat_srw_usd_t` | Пшеница, US Soft Red Winter | 0% | — |
| `barley_usd_t` | Ячмень | 15.7% | Pink Sheet не публиковал котировку ячменя часть лет (проверить конкретные года в `output/02_worldbank_prices.csv`) |
| `maize_usd_t` | Кукуруза | 0% | — |
| `rapeseed_oil_usd_t` | Рапсовое масло | 30.4% | Ряд начинается позже 1990 г. у источника |
| `sunflower_oil_usd_t` | Подсолнечное масло | 30.4% | Ряд начинается позже 1990 г. у источника |
| `soybean_oil_usd_t` | Соевое масло | 0% | — |
| `palm_oil_usd_t` | Пальмовое масло | 0% | — |
| `dap_fertilizer_usd_t` | Удобрение DAP | 0% | — |
| `urea_fertilizer_usd_t` | Удобрение — карбамид | 0% | — |

## Климат (ERA5-Land, скрипт 03)

Агрегат за вегетационный период (апрель–сентябрь), усреднён по bounding box зернового пояса (N55 S49 W60 E78) — **варьируется только по `year`**, одинаков для всех регионов. Пропуски 1.3% — годы за пределами покрытия ERA5-кэша на момент сборки.

| Столбец | Единица | Описание |
|---|---|---|
| `gs_t2m_kelvin` | K | Температура воздуха на 2м, сырое значение ERA5 |
| `gs_t2m_celsius` | °C | То же, в °C (= gs_t2m_kelvin − 273.15) |
| `gs_precip_m` | м | Осадки, сырое значение ERA5 (уже скорректировано на число дней в месяце — см. Отчёт §2.3) |
| `gs_precip_mm` | мм | То же, в мм (= gs_precip_m × 1000) |
| `gs_pot_evap_m` | м | Потенциальная эвапотранспирация, сырое значение |
| `gs_pot_evap_mm` | мм | То же, в мм |
| `gs_soil_water_l1` | м³/м³ | Объёмная влажность почвы, слой 1 (0–7см) |
| `gs_soil_water_l2` | м³/м³ | Объёмная влажность почвы, слой 2 (7–28см) |
| `gs_solar_rad_jm2` | Дж/м² | Приходящая солнечная радиация, сырое значение |
| `gs_solar_rad_mjm2` | МДж/м² | То же, в МДж/м² |

*Столбцы в Дж/К/м — сырые единицы ERA5 до пересчёта, оставлены для прозрачности; для анализа обычно нужны `_celsius`/`_mm`/`_mjm2`-варианты.*

## Почва (SoilGrids v2.0, скрипт 04)

**Одно значение на весь датасет** (среднее по 20 точкам сетки зернового пояса, слой 0–5см) — не варьируется ни по году, ни по региону, ни по культуре. Пропусков нет (константа).

| Столбец | Единица | Описание | Значение в датасете |
|---|---|---|---|
| `soil_phh2o` | pH | Кислотность (H₂O) | 7.04 |
| `soil_soc` | г/кг | Органический углерод | 62.9 |
| `soil_bdod` | г/см³ | Объёмная плотность | 1.05 |
| `soil_clay` | % | Содержание глины | 26.2 |
| `soil_sand` | % | Содержание песка | 41.5 |
| `soil_silt` | % | Содержание ила | 32.4 (сумма clay+sand+silt = 100%) |
| `soil_cec` | смоль(+)/кг | Ёмкость катионного обмена | 33.7 |
| `soil_nitrogen` | г/кг | Общий азот | 5.0 |

*Среднее по всем 20 точкам сетки (см. `Словарь_данных_output.md`, раздел 04 — после добавления повтора при сбое API все 20/20 точек собираются стабильно; до 2026-09-11 иногда усреднялось по 17/20 из-за не повторённых при сбое запросов).*

## Лаговые и скользящие признаки (считаются в 06_merge.py)

Группировка — по (`region`, `crop_en`), сортировка по `year`. Пропуски — ожидаемые (первые N лет каждого (регион, культура)-ряда физически не имеют предыдущих N наблюдений).

| Столбец | Описание | Пропуски |
|---|---|---|
| `yield_kgha_lag1` | Урожайность год назад | 5.5% |
| `yield_kgha_lag2` | Урожайность 2 года назад | 8.6% |
| `yield_kgha_lag3` | Урожайность 3 года назад | 11.8% |
| `yield_kgha_roll3` | Скользящее среднее урожайности за 3 предыдущих года (без текущего) | 12.8% |
| `yield_kgha_roll5` | Скользящее среднее урожайности за 5 предыдущих лет (без текущего) | 19.6% |
| `wheat_price_lag1` | Мировая цена пшеницы (`wheat_hrw_usd_t`) год назад | 3.1% |
| `wheat_price_lag2` | Мировая цена пшеницы 2 года назад | 6.3% |
| `wheat_price_lag3` | Мировая цена пшеницы 3 года назад | 9.4% |

*`wheat_price_lag*` считается с той же группировкой (region, crop_en), что и yield-лаги, хотя сама цена от региона/культуры не зависит (см. выше) — значение лага при этом всё равно корректно, просто одинаково для всех культур в рамках одного региона/года.*

---

## Быстрые проверки при использовании

```python
import pandas as pd
m = pd.read_csv("output/master_dataset.csv")

# Климат/цены не различаются по региону — не пытаться интерпретировать
# межрегиональные различия urrожайности через них напрямую
assert m.groupby("year")["gs_t2m_celsius"].nunique().max() == 1
assert m.groupby("year")["wheat_hrw_usd_t"].nunique().max() == 1

# Акмолинская "группа" культур не складывается 1:1 с отдельными культурами
# других областей — фильтровать по source, если нужна чистая выборка
m_clean_crops = m[m["source"] == "stat_gov_kz_real"]
```

См. также `Отчёт_о_сборе_данных.md` — полное описание источников и найденных при сборе ограничений/багов.
